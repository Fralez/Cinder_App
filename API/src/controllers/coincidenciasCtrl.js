const { Usuario } = require('../models/usuario');

module.exports = {
  coincidencias: async (req, res, next) => {

    /**
     * PASOS A SEGUIR:
     * 
     */

    const { idusuario } = req.params;

    
  }
}